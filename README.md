# Call Me App – React + TypeScript + Vite

Минимальная база для приложения на React с TypeScript и Vite, настроенная для стабильной разработки с поддержкой:

- **Hot Module Replacement (HMR)**
- **ESLint + Prettier** с автоматическим исправлением кода при коммитах
- **Husky + lint-staged** для проверки и форматирования кода перед коммитом
- **Sass** для модульных стилей компонентов

---

## 📦 Install dependencies

```bash
# Install dependencies
npm install
Все зависимости зафиксированы, чтобы гарантировать одинаковую сборку на всех машинах.
```

## 💻 Development

```bash
# Launching the dev-server
npm run dev

# Build the project
npm run build

# Viewing the build locally
npm run preview
```

## 🛠️ Linting and formatting

```bash
# Checking code with ESLint
npm run lint

# Formatting code with Prettier
npm run format
```
